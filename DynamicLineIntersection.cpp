#include <bits/stdc++.h>
using namespace std;
void dynamicLineIntersection(int n) {
    
}
int main() {
    int n;
    cin >> n;
    cin.ignore(numeric_limits<streamsize>::max(), '\n');
    dynamicLineIntersection(n);
    return 0;
}
