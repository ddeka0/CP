#include <bits/stdc++.h>
using namespace std;
// Find minimum in all the range of length k = 5
int main() {
	vector<int> v = {5,3,2,1,4,6,9,7,10,8};
	queue<int> Q;
	
	for(int i = 0,j = 4;j < v.size();) {
		// when i + 2 < j
		if(i + 4 < j) {
			if(v[i])
			j++:
		}
	}
}