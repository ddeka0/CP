#include <bits/stdc++.h>
using namespace std;
int a[10005];
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> N >> K;
    for(int i = 0;i<N;i++) {
        cin >> a[i];
    }
    sort(a,a+n);

}
